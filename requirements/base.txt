# GradeMIND — core API dependencies.
#
# Everything the FastAPI service needs to start, serve, authenticate, and
# talk to the database. No ML, no OCR, no PDF generation.
#
# Every direct dependency is pinned to an exact version. A range means two
# machines can resolve different code, and "reproducible from stored versions"
# (master spec Phase 2.6) is not achievable on top of a moving dependency set.
#
# Install:  pip install -r requirements/base.txt

fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
email-validator==2.2.0
python-multipart==0.0.20
httpx==0.28.1
PyYAML==6.0.2

# Async Task Queue
celery==5.4.0
redis==5.2.1
